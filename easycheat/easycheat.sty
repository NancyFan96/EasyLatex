\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{easycheat}[2019/04/06 EasyCheat package]


\RequirePackage{multicol}
\RequirePackage{calc}
\RequirePackage{ifthen}
\RequirePackage[landscape]{geometry}
\RequirePackage[]{hyperref}
\RequirePackage[]{graphicx}
\usepackage[font=small]{caption}

% self defined for cheat sheet
\RequirePackage[outline]{contour}
\RequirePackage[document]{ragged2e}
\RequirePackage[dvipsnames]{xcolor}

\newcommand{\lec}[1]{\colorbox{GreenYellow}{\textbf{#1}}}
\newcommand{\tab}[1]{\colorbox{SkyBlue}{#1}}
\newcommand{\tabtab}[1]{\colorbox{SkyBlue}{\texttt{>}}\textbf{#1}}
\newcommand{\pt}{ \smaller{\textcolor{NavyBlue}{\texttt{>>}}} }

\newcommand{\keya}[1]{\colorbox{Goldenrod}{#1}}
\newcommand{\keyb}[1]{\colorbox{Yellow}{#1}}
\newcommand{\keyc}[1]{\colorbox{Lavender}{#1}}

\newcommand{\nn}{\contour{black}{$\mid$}}
\newcommand{\sto}{\contour{black}{$\to$}}
\newcommand{\bto}{\contour{black}{$\leftrightarrow$}}
\newcommand{\sfrom}{\contour{black}{$\leftarrow$}}



% This sets page margins to .5 inch if using letter paper, and to 1cm
% if using A4 paper. (This probably isn't strictly necessary.)
% If using another size paper, use default 1cm margins.
\ifthenelse{\lengthtest { \paperwidth = 11in}}
	{ \geometry{top=.16in,left=.16in,right=.16in,bottom=.16in} }
	{\ifthenelse{ \lengthtest{ \paperwidth = 297mm}}
		{\geometry{top=1cm,left=1cm,right=1cm,bottom=1cm} }
		{\geometry{top=1cm,left=1cm,right=1cm,bottom=1cm} }
	}

% Turn off header and footer
\pagestyle{empty}
 

% Redefine section commands to use less space
\makeatletter
\renewcommand{\section}{\@startsection{section}{1}{0mm}%
                                {-1ex plus -.5ex minus -.2ex}%
                                {0.5ex plus .2ex}%x
                                {\normalfont\large\bfseries}}
\renewcommand{\subsection}{\@startsection{subsection}{2}{0mm}%
                                {-1explus -.5ex minus -.2ex}%
                                {0.5ex plus .2ex}%
                                {\normalfont\normalsize\bfseries}}
\renewcommand{\subsubsection}{\@startsection{subsubsection}{3}{0mm}%
                                {-1ex plus -.5ex minus -.2ex}%
                                {1ex plus .2ex}%
                                {\normalfont\small\bfseries}}
\makeatother

\def\dashdotted{\xleaders\hbox to 1em{$- \cdot$}\hfill $-$}
\def\columnseprulecolor{%
	\rotatebox{90}{\makebox[\textheight][c]{\textcolor{lightgray}{\dashdotted}} }
}%

\setlength{\columnseprule}{0pt}
\setlength{\columnsep}{5pt}
\setlength{\parindent}{0pt}
\setlength{\parskip}{0pt plus 0.5ex}

% Define BibTeX command
\def\BibTeX{{\rm B\kern-.05em{\sc i\kern-.025em b}\kern-.08em
		T\kern-.1667em\lower.7ex\hbox{E}\kern-.125emX}}

% Don't print section numbers
\setcounter{secnumdepth}{0}
